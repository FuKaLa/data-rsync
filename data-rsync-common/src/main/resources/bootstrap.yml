# Bootstrap配置文件
spring:
  application:
    name: data-rsync-common
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: dev
        group: DEFAULT_GROUP
        file-extension: yml
        username: nacos
        password: nacos
      discovery:
        server-addr: localhost:8848
        namespace: dev
        group: DEFAULT_GROUP
        username: nacos
        password: nacos

# 环境变量配置
---
spring:
  profiles:
    active: dev